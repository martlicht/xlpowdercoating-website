---
import { Image } from "astro:assets";
import backgroundImage from "../assets/images/background4.webp";
---

<section
  class="relative min-h-screen/2 flex items-center py-20 px-4 md:px-8 lg:px-16 font-sans overflow-hidden"
>
  <!-- Background Image -->
  <div class="absolute inset-0 z-0" aria-hidden="true">
    <Image
      src={backgroundImage}
      alt="XL Industrial Powder Coating industries background"
      format="webp"
      quality={80}
      class="w-full h-full object-cover"
      loading="lazy"
    />
    <div class="absolute inset-0 bg-black/80 z-10"></div>
  </div>

  <!-- Content Container -->
  <div
    class="relative z-20 w-full px-8 md:px-12 lg:px-16 xl:px-24 flex flex-col items-center text-center"
  >
    <!-- Small Label -->
    <p
      class="text-sm md:text-base uppercase tracking-wider text-white/80 mb-4 font-sans"
    >
      Industries
    </p>

    <!-- Main Title -->
    <h2
      class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl xl:text-6xl font-bold uppercase leading-tight italic text-white mb-6 md:mb-8 font-sans max-w-5xl"
      data-aos="fade-up"
      data-aos-duration="500"
    >
      BUILT FOR EVERY SECTOR THAT<br />
      <span class="text-white">DEMANDS</span>{" "}
      <span class="text-[#DC2626]">DURABILITY</span>
    </h2>

    <!-- Description -->
    <p
      class="text-base sm:text-base md:text-lg lg:text-xl text-white/90 max-w-4xl leading-relaxed font-sans"
    >
      Automotive shops, manufacturing plants, construction companies, furniture
      makers, and signage producers trust us with their toughest jobs.
    </p>
  </div>
</section>
